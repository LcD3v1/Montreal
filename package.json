{
  "name": "sistema-montreal",
  "version": "1.0.0",
  "description": "Sistema de Gestão Operacional — Facção Montreal",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm run install:all && cd frontend && npm run build && cd ../backend && npm run build",
    "start": "NODE_ENV=production node backend/dist/index.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
